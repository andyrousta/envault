export { vaultPath, vaultExists, writeVault, readVaultRaw, readVault } from './vaultFile';
export {
  setEntry,
  getEntry,
  deleteEntry,
  listEntries,
  type VaultEntry,
  type VaultData,
} from './vaultEntry';
